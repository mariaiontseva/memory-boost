# Архитектура продукта Memory Boost

## Содержание
1. [Обзор системы](#обзор-системы)
2. [Архитектурная схема](#архитектурная-схема)
3. [Технологический стек](#технологический-стек)
4. [API Endpoints](#api-endpoints)
5. [Структура базы данных](#структура-базы-данных)
6. [Интеграция с AI](#интеграция-с-ai)
7. [Roadmap](#roadmap)
8. [Unit Economics](#unit-economics)

---

## Обзор системы

Memory Boost — это SaaS-платформа для ускоренного обучения, использующая AI для автоматического создания учебных материалов с научно обоснованными методами запоминания.

### Основные функции:
- 📄 Загрузка и обработка PDF/изображений
- 🤖 AI-анализ и структурирование контента
- 🎨 Автоматическое визуальное кодирование
- 🧠 Генерация мнемоник
- ✅ Создание тестов (multiple choice, fill-in-blank)
- ⏰ Интервальное повторение (SM-2)
- 📊 Аналитика и отслеживание прогресса

---

## Архитектурная схема

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │   Web App   │  │  Mobile App │  │   PWA       │              │
│  │   (React)   │  │ (React Nat.)│  │             │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          │                                       │
│                          ▼                                       │
│                   ┌──────────────┐                               │
│                   │   API Gateway│                               │
│                   │   (nginx)    │                               │
│                   └──────┬───────┘                               │
└──────────────────────────┼──────────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────────┐
│                          ▼            APPLICATION LAYER          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    FastAPI Backend                         │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │  │
│  │  │  Auth   │ │ Content │ │  Quiz   │ │ Progress│         │  │
│  │  │ Service │ │ Service │ │ Service │ │ Service │         │  │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘         │  │
│  └───────┼──────────┼──────────┼──────────┼─────────────────┘  │
│          │          │          │          │                     │
│          └──────────┼──────────┼──────────┘                     │
│                     │          │                                 │
│          ┌──────────┴──────────┴──────────┐                     │
│          │      Background Workers         │                     │
│          │         (Celery)                │                     │
│          └─────────────┬──────────────────┘                     │
└────────────────────────┼────────────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────────────┐
│                        ▼              DATA LAYER                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ PostgreSQL  │  │    Redis    │  │     S3      │              │
│  │  (Primary)  │  │   (Cache)   │  │  (Storage)  │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────────────┐
│                        ▼            EXTERNAL SERVICES            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Claude API  │  │ Stripe API  │  │  SendGrid   │              │
│  │    (AI)     │  │ (Payments)  │  │  (Email)    │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Технологический стек

### Frontend
| Технология | Назначение |
|------------|------------|
| **React 18** | UI framework |
| **TypeScript** | Type safety |
| **Tailwind CSS** | Styling |
| **Zustand** | State management |
| **React Query** | Data fetching |
| **Vite** | Build tool |

### Backend
| Технология | Назначение |
|------------|------------|
| **FastAPI** | Web framework |
| **Python 3.11+** | Runtime |
| **Pydantic** | Data validation |
| **SQLAlchemy** | ORM |
| **Alembic** | Migrations |
| **Celery** | Task queue |

### Database & Storage
| Технология | Назначение |
|------------|------------|
| **PostgreSQL 15** | Primary database |
| **Redis** | Caching, sessions |
| **AWS S3** | File storage |
| **Pinecone** | Vector database (для AI поиска) |

### Infrastructure
| Технология | Назначение |
|------------|------------|
| **Docker** | Containerization |
| **Kubernetes** | Orchestration |
| **AWS EKS** | Managed K8s |
| **GitHub Actions** | CI/CD |
| **Sentry** | Error tracking |
| **Datadog** | Monitoring |

---

## API Endpoints

### Authentication
```
POST   /api/v1/auth/register     # Регистрация
POST   /api/v1/auth/login        # Вход
POST   /api/v1/auth/logout       # Выход
POST   /api/v1/auth/refresh      # Обновление токена
GET    /api/v1/auth/me           # Текущий пользователь
```

### Content
```
POST   /api/v1/content/upload    # Загрузка файла
POST   /api/v1/content/text      # Загрузка текста
GET    /api/v1/content/{id}      # Получение контента
GET    /api/v1/content/          # Список контента
DELETE /api/v1/content/{id}      # Удаление
POST   /api/v1/content/{id}/process  # Обработка AI
```

### Quiz
```
GET    /api/v1/quiz/{content_id}         # Получить тесты
POST   /api/v1/quiz/{content_id}/answer  # Ответить на вопрос
GET    /api/v1/quiz/stats                # Статистика тестов
```

### Spaced Repetition
```
GET    /api/v1/sr/due            # Карточки для повторения
POST   /api/v1/sr/review         # Отметить повторение
GET    /api/v1/sr/schedule       # График повторений
PUT    /api/v1/sr/settings       # Настройки SM-2
```

### Progress
```
GET    /api/v1/progress/         # Общий прогресс
GET    /api/v1/progress/topics   # По темам
GET    /api/v1/progress/streak   # Серия дней
GET    /api/v1/progress/export   # Экспорт данных
```

---

## Структура базы данных

### Основные таблицы

```sql
-- Пользователи
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    name VARCHAR(255),
    plan VARCHAR(50) DEFAULT 'free',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Контент
CREATE TABLE content (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(500),
    original_text TEXT,
    processed_text JSONB,  -- Структурированный контент
    mnemonics JSONB,       -- Сгенерированные мнемоники
    file_url VARCHAR(1000),
    status VARCHAR(50) DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT NOW()
);

-- Карточки для повторения
CREATE TABLE cards (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    content_id UUID REFERENCES content(id) ON DELETE CASCADE,
    front TEXT NOT NULL,
    back TEXT NOT NULL,
    card_type VARCHAR(50),  -- concept, definition, formula, example
    -- SM-2 параметры
    easiness_factor FLOAT DEFAULT 2.5,
    interval_days INTEGER DEFAULT 0,
    repetitions INTEGER DEFAULT 0,
    next_review TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Тесты
CREATE TABLE quizzes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    content_id UUID REFERENCES content(id) ON DELETE CASCADE,
    question TEXT NOT NULL,
    quiz_type VARCHAR(50),  -- multiple_choice, fill_blank
    options JSONB,
    correct_answer TEXT,
    explanation TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Ответы на тесты
CREATE TABLE quiz_attempts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    quiz_id UUID REFERENCES quizzes(id) ON DELETE CASCADE,
    answer TEXT,
    is_correct BOOLEAN,
    time_spent_seconds INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Прогресс
CREATE TABLE progress (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    content_id UUID REFERENCES content(id) ON DELETE CASCADE,
    mastery_level FLOAT DEFAULT 0,  -- 0-100
    total_reviews INTEGER DEFAULT 0,
    correct_reviews INTEGER DEFAULT 0,
    last_reviewed TIMESTAMP,
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_cards_next_review ON cards(next_review);
CREATE INDEX idx_content_user ON content(user_id);
CREATE INDEX idx_progress_user ON progress(user_id);
```

---

## Интеграция с AI

### Claude API для обработки контента

```python
from anthropic import Anthropic

client = Anthropic()

async def process_content(text: str) -> dict:
    """Обработка текста с помощью Claude"""

    prompt = """Проанализируй учебный текст и верни JSON:
    {
        "structured_content": {
            "key_concepts": [...],     // Ключевые понятия
            "definitions": [...],      // Определения
            "formulas": [...],         // Формулы
            "examples": [...]          // Примеры
        },
        "mnemonics": [
            {
                "type": "acronym|association|rhyme",
                "text": "...",
                "explanation": "..."
            }
        ],
        "quiz_questions": [
            {
                "type": "multiple_choice|fill_blank",
                "question": "...",
                "options": [...],
                "correct_answer": "...",
                "explanation": "..."
            }
        ],
        "flashcards": [
            {
                "front": "...",
                "back": "...",
                "type": "concept|definition|formula|example"
            }
        ]
    }
    """

    response = await client.messages.create(
        model="claude-sonnet-4-20250514",
        max_tokens=4096,
        messages=[
            {"role": "user", "content": f"{prompt}\n\nТекст:\n{text}"}
        ]
    )

    return json.loads(response.content[0].text)
```

### OCR для PDF/изображений

```python
import anthropic

async def extract_text_from_image(image_base64: str) -> str:
    """Извлечение текста из изображения с помощью Claude Vision"""

    response = await client.messages.create(
        model="claude-sonnet-4-20250514",
        max_tokens=4096,
        messages=[
            {
                "role": "user",
                "content": [
                    {
                        "type": "image",
                        "source": {
                            "type": "base64",
                            "media_type": "image/jpeg",
                            "data": image_base64
                        }
                    },
                    {
                        "type": "text",
                        "text": "Извлеки весь текст с этого изображения. Сохрани форматирование."
                    }
                ]
            }
        ]
    )

    return response.content[0].text
```

---

## Roadmap

### Phase 1: MVP (Месяц 1-2)
- [ ] Базовая авторизация
- [ ] Загрузка текста и обработка AI
- [ ] Цветовое кодирование контента
- [ ] Простые тесты (multiple choice)
- [ ] Базовый интерфейс

### Phase 2: Core Features (Месяц 3-4)
- [ ] Загрузка PDF/изображений
- [ ] Генерация мнемоник
- [ ] Fill-in-blank тесты
- [ ] SM-2 алгоритм
- [ ] Уведомления о повторении

### Phase 3: Growth (Месяц 5-6)
- [ ] Мобильное приложение (React Native)
- [ ] Социальные функции (шаринг материалов)
- [ ] Интеграция с LMS
- [ ] API для партнёров
- [ ] Gamification (достижения, streak)

### Phase 4: Scale (Месяц 7-12)
- [ ] Enterprise версия
- [ ] White-label решение
- [ ] Advanced аналитика
- [ ] AI-tutor (интерактивные объяснения)
- [ ] Интеграция с Notion/Obsidian

---

## Unit Economics

### Ценовые планы

| План | Цена/мес | Лимиты |
|------|----------|--------|
| **Free** | $0 | 3 документа, 50 карточек |
| **Pro** | $9.99 | Безлимит документов, 1000 карточек |
| **Team** | $7.99/user | Всё из Pro + collaboration |
| **Enterprise** | Custom | SSO, API, SLA |

### Метрики

```
Предположения:
- CAC (Customer Acquisition Cost): $15
- Monthly Churn: 5%
- LTV (Lifetime Value): $9.99 × 20 месяцев = $200
- LTV/CAC ratio: 13.3x ✓ (>3x хорошо)

Структура затрат (на 10,000 MAU):
┌─────────────────────────────────┬──────────┐
│ Статья                          │ $/месяц  │
├─────────────────────────────────┼──────────┤
│ Claude API (~$0.003/request)    │ $3,000   │
│ Infrastructure (AWS)            │ $2,000   │
│ Stripe fees (2.9% + $0.30)      │ $1,500   │
│ Other SaaS (Sentry, etc.)       │ $500     │
├─────────────────────────────────┼──────────┤
│ Total COGS                      │ $7,000   │
│ Gross Revenue (30% conversion)  │ $30,000  │
│ Gross Margin                    │ 77%      │
└─────────────────────────────────┴──────────┘
```

### Ключевые KPI

| Метрика | Target |
|---------|--------|
| DAU/MAU | >40% |
| Retention D7 | >50% |
| Retention D30 | >30% |
| Free → Pro conversion | >5% |
| NPS | >50 |

---

## Заключение

Memory Boost строится на **проверенной архитектуре** современных SaaS-продуктов:

1. **Масштабируемость** — микросервисы и Kubernetes
2. **Надёжность** — мониторинг и автоматическое восстановление
3. **Безопасность** — OAuth 2.0, шифрование, GDPR compliance
4. **Гибкость** — API-first подход для интеграций

Техническая архитектура позволяет быстро итерировать на MVP и масштабироваться до миллионов пользователей.
